{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://prebid.org/schemas/pbs/colossus.json",
  "title": "Colossus bidder params (Prebid Server)",
  "description": "A schema which validates params accepted by the Colossus adapter",
  "type": "object",
  "properties": {
    "TagID": {
      "type": "string",
      "description": "An ID which identifies the colossus ad tag"
    },
    "groupId": {
      "type": "string",
      "description": "An ID which identifies the colossus group"
    }
  },
  "oneOf": [
    {
      "required": [
        "TagID"
      ]
    },
    {
      "required": [
        "groupId"
      ]
    }
  ]
}
