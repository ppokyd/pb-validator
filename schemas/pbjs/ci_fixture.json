{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://prebid.org/schemas/pbjs/ci_fixture.json",
  "title": "CI fixture bidder params",
  "description": "Strict schema used only in tests (required token, no extra properties).",
  "type": "object",
  "additionalProperties": false,
  "required": ["token"],
  "properties": {
    "token": { "type": "string", "minLength": 1 }
  }
}
